/// Human-readable build marker, shown on the splash and login screens so a
/// tester can confirm at a glance WHICH build is actually running on their
/// device (removes the "did the new APK really install?" ambiguity).
/// Bump this together with `version:` in pubspec.yaml on every test build.
const String kBuildTag = 'build 3 · restore-probe';
